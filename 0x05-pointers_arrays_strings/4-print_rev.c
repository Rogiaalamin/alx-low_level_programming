#include "main.h"

/**
 * print_rev - check the code
 *
 * @s: is a parameter
 */

void print_rev(char *s)
{
	int i, j;

	for (i = 0; *s = '\0'; s++)

	j = s;

	while (j)
	{
		_putchar(*ptr);
		j--;
	}
	_putchar('\n');
}

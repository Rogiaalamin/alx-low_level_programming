#include "main.h"

/**
 * print_rev - check the code
 *
 * @s: is a parameter
 */

void print_rev(char *s)
{
	int i;

	for (i = 0; *s = '\0'; s++)

	while (j)
	{
		_putchar(*s);
		s--;
	}
	_putchar('\n');
}
